---
description: Editor-only widget subclasses that surface otherwise-protected engine APIs.
sidebar_position: 1
---

import DocCardList from '@theme/DocCardList';

# Components

Editor-only widget subclasses — typically thin wrappers that surface otherwise-`protected` engine delegates so native callers can subscribe to them. Mirrors the layout of `NexusUIEditor/Public/Components/`.

<DocCardList />
