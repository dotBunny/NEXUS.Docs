---
description: Scene-component subclasses with replication and authority-aware setters.
sidebar_position: 2
---

import DocCardList from '@theme/DocCardList';

# Scene Components

`USceneComponent` subclasses — typically thin extensions on stock components that add replication, authority-aware setters, or fill in missing engine APIs. Mirrors the layout of `NexusUI/Public/SceneComponents/`.

<DocCardList />
