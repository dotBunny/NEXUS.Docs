---
description: Editor component visualizers that draw Actor Pools shapes into level viewports.
sidebar_position: 1
---

import DocCardList from '@theme/DocCardList';

# Visualizers

`FComponentVisualizer` subclasses that draw Actor Pools-related shapes into level viewports while the editor is open. Mirrors the layout of `NexusActorPoolsEditor/Public/Visualizers/`.

<DocCardList />
