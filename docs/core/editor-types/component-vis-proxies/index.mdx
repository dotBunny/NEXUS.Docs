---
description: Hit-proxy structs used by NEXUS component visualizers to identify picked elements.
sidebar_position: 1
---

import DocCardList from '@theme/DocCardList';

# Component Vis Proxies

Hit-proxy structs used by NEXUS component visualizers to identify picked elements (vertices, edges) inside the editor viewport. Mirrors the layout of `NexusCoreEditor/Public/ComponentVisProxies/`.

<DocCardList />
