---
description: UAssetDefinition subclasses that register Actor Pools assets with the Content Browser.
sidebar_position: 1
---

import DocCardList from '@theme/DocCardList';

# Asset Definitions

`UAssetDefinition` subclasses that register Actor Pools assets with the Content Browser — supplying display names, category paths, asset colors, and validation hooks. Mirrors the layout of `NexusActorPoolsEditor/Public/AssetDefinitions/`.

<DocCardList />
