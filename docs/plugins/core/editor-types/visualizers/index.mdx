---
description: Editor component visualizers that draw Core marker components into level viewports.
sidebar_position: 1
---

import DocCardList from '@theme/DocCardList';

# Visualizers

`FComponentVisualizer` subclasses that draw Core's marker components into level viewports while the editor is open. Mirrors the layout of `NexusCoreEditor/Public/Visualizers/`.

<DocCardList />
