---
description: Reusable composite widgets — list-view entries and the developer-overlay base class.
sidebar_position: 2
---

import DocCardList from '@theme/DocCardList';

# Widgets

Reusable composite `UUserWidget` subclasses — list-view entry rows and the `UNDeveloperOverlay` base every plugin's diagnostic surface derives from. Mirrors the layout of `NexusUI/Public/Widgets/`.

<DocCardList />
