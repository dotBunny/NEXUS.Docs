---
description: The editor mode used to author and inspect cells, junctions and organs without leaving the level viewport.
sidebar_position: 4
---

import DocCardList from '@theme/DocCardList';

# Editor Mode

World Assembly ships an editor mode (`UNWorldAssemblyEdMode`) that puts cell, junction and organ authoring into the level editor. Its UI lives in the **Mode Toolbox** panel rather than the viewport: a vertical rail of category buttons, the selected category's tools and commands beside it, and a warning area beneath.

![The World Assembly toolkit panel, showing the category rail and the Cell category's sections](/assets/images/docs/plugins/world-assembly/editor-mode/rail-cell.webp)

## Entering the Mode

A single entry point sits on the level-editor toolbar. The whole NEXUS section — the mode button and the [Quick Assembly](#quick-assembly) controls — appears only when the current level contains **either** a `ANCellActor` **or** a `UNOrganComponent`; in an unrelated level it collapses entirely.

![The World Assembly mode button on the level-editor toolbar](/assets/images/docs/plugins/world-assembly/editor-mode/mode-button.webp)

Above is a cell-only level: the mode button is there, and the Quick Assembly controls beside it have collapsed because no organ is present.

## Quick Assembly

Quick Assembly is **not** part of the mode's panel — it stays on the level-editor toolbar and works whether or not the mode is open. It requires at least one [Organ](../types/organ-volume.md) registered in the world; with only a cell actor present the controls collapse and the mode button appears on its own.

![The Quick Assembly organ picker and start button on the level-editor toolbar](/assets/images/docs/plugins/world-assembly/editor-mode/quick-assembly-toolbar.webp)

Select an [Organ](../types/organ-volume.md) from the combo box and start an operation. The resulting run carries the placed [Cells](../types/cell.md) through `ANCellProxy` to the actualized `ANCellLevelInstance`. While it runs, the button becomes a cancel action and its progress bar tracks the operation.

The options dropdown (`⋮`) beside the start/cancel button exposes quick toggles that mirror [User Settings → Quick Assembly](../user-settings.md#quick-assembly):

- **Load Level Instances** — carry each run through to actualized `ANCellLevelInstance`s rather than stopping at `ANCellProxy`.
- **Auto Assembly** — after each run finishes, re-trigger the operation on a timer (the **Auto Assembly Timer**, 2–180s) and loop until cancelled. While looping, the button stays in its cancel state and its progress bar shows the countdown between runs. When you stop the loop a single summary toast reports the accumulated pass / warning / fail counts, total cells created, and total time — provided **Toast Editor Assembly Operations** is enabled in the [User Settings](../user-settings.md#notifications).

:::info

**Fillers** are not placed outside of `PIE` or runtime; optionally the unfilled junctions can have their gizmos colored differently in this situation.

:::

## The Panel

Each rail category renders the same way top to bottom:

| Part | What it holds |
| :-- | :-- |
| **Rail** | The category buttons, down the left edge. The selected one stays lit. |
| **Header** | A picker naming what the category acts on — the [Junction](junction.md) and [Organ](organ.md) rails have one; [World](world.md) and [Cell](cell.md) do not. |
| **Content** | The category's titled sections of tools, commands and toggles. |
| **Tool area** | The running tool's header and its property details, when one is running. |
| **Warnings** | The focused cell's outstanding warnings, pinned at the bottom. |

### The Rail

Categories appear in this order, and a category whose requirements are unmet is **greyed out rather than hidden** — so an unavailable category still says it exists.

| Category | Purpose | Enabled when |
| :-- | :-- | :-- |
| [World](world.md) | World-scoped tools: the collision visualizer and the actor-ignore tagging toggles. | Always. |
| [Cell](cell.md) | Author the focused cell: edit its bounds, hull and voxel data, and manage its side-car asset. | A `ANCellActor` is present. |
| [Junction](junction.md) | Place and align the focused cell's junctions. | A `ANCellActor` is present. |
| [Organ](organ.md) | Generate and clear organ proxies, and manage their level instances. | A `UNOrganComponent` is present in the world. |

### Warnings

The warning area reports everything currently applying to the focused cell — unsaved cell data, and each of the auto-calculate options that has been switched off. It collapses to nothing when there is nothing to report, which is why it is absent from the captures on these pages.

Earlier versions drew this text over the viewport canvas; it moved into the panel, where it no longer competes with the scene or has to be suppressed to take a clean screenshot.

## Tools

Four of the five interactive tools ship visible: [Bounds](cell.md#bounds), [Vertices](cell.md#vertices) and [Edges](cell.md#edges) on the Cell rail, and [Place](junction.md#place) on the Junction rail. The voxel tool is [currently hidden](cell.md#voxel-data).

Three rules apply to all of them:

- **They commit as they go.** Every edit is applied as it is made, so there is nothing to accept or discard on the way out — leaving is the whole gesture.
- **`Esc` ends the running tool.** Because edits are already committed, this is a stop rather than a cancel.
- **Switching rail category ends the running tool too.** Tools are scoped to the category that presents them, so one left running under a different category would keep drawing handles and swallowing viewport clicks with nothing on screen to say why.

<DocCardList />
